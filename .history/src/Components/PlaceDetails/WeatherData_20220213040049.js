import React from 'react'

function WeatherData() {
  
      {WeatherData.main((weather)=>{
        console.log(weather);
      })}
        
        return (

            <div>{weather}</div>

  )
}

export default WeatherData